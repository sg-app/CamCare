<RadzenButton class="my-1" Click="InsertClick" Icon="add" Text="Einfügen" Size="ButtonSize.Small" Disabled="@Disabled" />

@code
{
    [Parameter]
    public EventCallback<MouseEventArgs> InsertClick { get; set; }
    [Parameter]
    public bool Disabled { get; set; }
}